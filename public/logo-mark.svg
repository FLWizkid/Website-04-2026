<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120">
  <!-- White background -->
  <circle cx="60" cy="60" r="58" fill="#FFFFFF"/>

  <!-- Dark blue left circle (larger, bolder) -->
  <circle cx="45" cy="50" r="32" fill="#0047A3"/>

  <!-- Teal right circle (larger, bolder) -->
  <circle cx="75" cy="50" r="32" fill="#14B8A6"/>

  <!-- Dark blue accent at bottom left -->
  <circle cx="35" cy="80" r="18" fill="#001D47"/>
</svg>
